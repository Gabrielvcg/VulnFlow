# Production deployment bundle

The files in this directory are synchronized to the VPS by GitHub Actions.
They are not a local-development Compose replacement and contain no production
secrets.

Follow the canonical [VPS deployment runbook](../docs/operations/vps-deployment.md)
before enabling the production job or changing runtime state.
