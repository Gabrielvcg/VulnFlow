# Local infrastructure

The local PostgreSQL and backend services are defined in the repository root
`docker-compose.yml`. Additional local infrastructure belongs here only when a
concrete requirement appears.

